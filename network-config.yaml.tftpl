network:
  version: 2
  ethernets:
    ${interface}:
      dhcp4: false
      addresses:
        - ${ip_address}/${netmask}
      routes:
        - to: default
          via: ${gateway}
      nameservers:
        addresses: [${dns_server}] 